# ============================================================
# .env.development — Development Environment
# Used when NODE_ENV=development
# ============================================================

NODE_ENV=development
PORT=3000
APP_VERSION=1.0.0

DB_HOST=localhost
DB_PORT=5432
DB_NAME=devops_node_db_dev
DB_USER=dev_user
DB_PASSWORD=dev_password

API_SECRET_KEY=dev_secret_key_not_for_production
