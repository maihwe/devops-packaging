# ============================================================
# .env.staging — Staging Environment
# Used when NODE_ENV=staging
# ============================================================

NODE_ENV=staging
PORT=3000
APP_VERSION=1.0.0

DB_HOST=staging-db.internal
DB_PORT=5432
DB_NAME=devops_node_db_staging
DB_USER=staging_user
DB_PASSWORD=staging_password

API_SECRET_KEY=staging_secret_key
