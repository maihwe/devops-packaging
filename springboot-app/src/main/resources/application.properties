# ============================================================
# application.properties — Base Configuration
# Shared across all environments
# ============================================================

app.name=devops-spring-app
app.version=1.0.0

spring.application.name=devops-spring-app
server.port=8080
